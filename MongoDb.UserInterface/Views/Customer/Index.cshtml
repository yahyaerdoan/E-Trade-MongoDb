@model List<ResultCustomerDto>
@{
    //ViewData["Title"] = "Index";
    Layout = "~/Views/Layout/Index.cshtml";
}

<link href="~/lib/bootstrap/dist/css/bootstrap.min.css" rel="stylesheet" />
<!DOCTYPE html>
<div class="container-fluid">
    <br />
    <div class="alert alert-primary">
        <h4>Customer List</h4>
    </div>
    <br />
    <table class="table table-hover">
        <tr>
            <th>Id</th>
            <th>First Name</th>
            <th>Last Name</th>
            <th>Email</th>
            <th>Address</th>
            <th>Delete</th>
            <th>Update</th>
        </tr>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.Id</td>
                <td>@item.FirstName</td>
                <td>@item.LastName</td>
                <td>@item.Email</td>
                <td>@item.Address</td>
                <td> <a href="/Customer/DeleteCustomer/@item.Id" class="btn  btn-danger">Delete</a></td>
                <td> <a href="/Customer/UpdateCustomer/@item.Id" class="btn  btn-success">Update</a></td>
            </tr>
        }
    </table>
    <a href="/Customer/CreateCustomer" class="btn  btn-success">New Customer</a>
</div>

